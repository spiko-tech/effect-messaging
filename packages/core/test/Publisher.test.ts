import { assert, describe, it } from "@effect/vitest"
import * as Publisher from "../src/Publisher.js"

describe("Publisher", () => {
  it("Dummy test", () => {
    assert.equal(Publisher.TypeId, Publisher.TypeId)
  })
})
