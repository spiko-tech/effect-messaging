import { assert, describe, it } from "@effect/vitest"
import * as Producer from "../src/Producer.js"

describe("Producer", () => {
  it("Dummy test", () => {
    assert.equal(Producer.TypeId, Producer.TypeId)
  })
})
