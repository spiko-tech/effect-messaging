/**
 * @since 0.3.0
 */
import * as internal from "./internal/SubscriberRunner.js"

/**
 * @since 0.3.0
 * @category execution
 */
export const runStream = internal.runStream

/**
 * @since 0.3.0
 * @category models
 */
export type SubscriberRunnerOptions = internal.SubscriberRunnerOptions
