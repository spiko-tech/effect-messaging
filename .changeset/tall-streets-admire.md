---
"@effect-messaging/core": major
"@effect-messaging/nats": major
"@effect-messaging/amqp": major
---

Release `@effect-messaging/core` 1.0.0-beta.0 with Effect 4.0.0-rc.112 while preserving the 0.2.43 publisher, subscriber, handler
timeout, shutdown, and tracing contracts.

Release `@effect-messaging/nats` 1.0.0-beta.0 with Effect 4.0.0-rc.112 while retaining the 0.7.6 SDK-backed Core NATS and
JetStream modules, public operations, connection lifecycle, publishing, subscriptions, management APIs, and tracing.

Release `@effect-messaging/amqp` 1.0.0-beta.0 with Effect 4.0.0-rc.112 while retaining the 0.6.5 `amqplib`-backed connection,
channel, publisher, subscriber, topology, settlement, tracing, cancellation, and reconnect behavior.
